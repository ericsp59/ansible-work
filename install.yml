- name: user
  hosts: demo
  tasks:
    - name: Install links
      apt:
        name: links
        state: present
        update-cache: yes
      become: true  
      register: out # регистрируем переменную для дебага
    - debug:
        var: out # вкл. дебаг и выводим в переменную 
    #   #debugger: always